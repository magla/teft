export function CTASection() {
  return (
    <section>
      <h2>Teft</h2>
      <p>As a result, when violet light strikes the eye, the S-cone is stimulated strongly and the L-cone is stimulated weakly.</p>
      <button type='button' className="btn" onClick={() => {}}>Why teft?</button>
    </section>
  );
}